<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Figures 3D Transparentes</title>

  <!-- Polices -->
  <link href="https://fonts.googleapis.com/css2?family=Androgy+Demo&display=swap" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Mabry+Pro&display=swap" rel="stylesheet" />

  <style>
    :root {
      --primary-color: #3a86ff;
      --secondary-color: #8338ec;
      --accent-color-1: #ff006e;
      --accent-color-2: #fb5607;
      --accent-color-3: #ffbe0b;
      --background-color: #f0f4f8;
      --cube-veil: rgba(58, 134, 255, 0.08);
      --border-radius: 8px;
      --transition: 0.3s ease;
    }

    body {
      font-family: 'Mabry Pro', sans-serif;
      background: var(--background-color);
      margin: 0;
      padding: 2rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      text-align: center;
      color: #212529;
      gap: 3rem;
    }

    h1 {
      font-family: 'Androgy Demo', sans-serif;
      font-size: 2.5rem;
      color: var(--primary-color);
      margin-bottom: 1rem;
    }

    .title {
      font-weight: bold;
      margin-bottom: 1.5rem;
      color: #212529;
    }

    /* General container */
    .shape-block {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    /* Cube Container */
    .cube-container, .pyramid-container {
      width: 200px;
      height: 200px;
      perspective: 800px;
    }

    .cube, .pyramid {
      width: 100%;
      height: 100%;
      position: relative;
      transform-style: preserve-3d;
      animation: rotateShape 20s linear infinite;
    }

    /* Pause on hover */
    .cube:hover, .pyramid:hover {
      animation-play-state: paused;
      cursor: grab;
    }

    /* Cube Faces */
    .face {
      position: absolute;
      width: 100%;
      height: 100%;
      background: var(--cube-veil);
      border: 2px solid;
      box-sizing: border-box;
      backdrop-filter: blur(2px);
      border-radius: var(--border-radius);
    }

    .front  { transform: translateZ(100px); border-color: var(--primary-color); }
    .back   { transform: rotateY(180deg) translateZ(100px); border-color: var(--secondary-color); }
    .right  { transform: rotateY(90deg) translateZ(100px); border-color: var(--accent-color-1); }
    .left   { transform: rotateY(-90deg) translateZ(100px); border-color: var(--accent-color-2); }
    .top    { transform: rotateX(90deg) translateZ(100px); border-color: var(--accent-color-3); }
    .bottom { transform: rotateX(-90deg) translateZ(100px); border-color: var(--primary-color); }

    /* Pyramid Base */
    .pyramid .base {
      position: absolute;
      width: 100%;
      height: 100%;
      background: var(--cube-veil);
      border: 2px solid var(--primary-color);
      border-radius: var(--border-radius);
      backdrop-filter: blur(2px);
      transform: rotateX(90deg) translateZ(-100px);
    }

    /* Pyramid Triangular Faces */
    .pyramid .side {
      position: absolute;
      width: 0;
      height: 0;
      border-style: solid;
      border-width: 0 100px 173.2px 100px; /* Triangle équilatéral approximé */
      background: transparent;
      border-bottom-color: var(--cube-veil);
      backdrop-filter: blur(2px);
    }

    .side1 { border-bottom-color: var(--primary-color); transform: rotateY(0deg) translateZ(100px) rotateX(60deg); }
    .side2 { border-bottom-color: var(--secondary-color); transform: rotateY(90deg) translateZ(100px) rotateX(60deg); }
    .side3 { border-bottom-color: var(--accent-color-1); transform: rotateY(180deg) translateZ(100px) rotateX(60deg); }
    .side4 { border-bottom-color: var(--accent-color-2); transform: rotateY(-90deg) translateZ(100px) rotateX(60deg); }

    @keyframes rotateShape {
      0%   { transform: rotateX(-20deg) rotateY(0deg); }
      100% { transform: rotateX(-20deg) rotateY(360deg); }
    }

    /* Responsive */
    @media (max-width: 600px) {
      .cube-container, .pyramid-container {
        width: 150px;
        height: 150px;
      }
    }
  </style>
</head>
<body>

  <div class="shape-block">
    <h1>Cube 3D Transparent</h1>
    <div class="cube-container">
      <div class="cube">
        <div class="face front"></div>
        <div class="face back"></div>
        <div class="face left"></div>
        <div class="face right"></div>
        <div class="face top"></div>
        <div class="face bottom"></div>
      </div>
    </div>
  </div>

  <div class="shape-block">
    <h1>Pyramide 3D Transparente</h1>
    <div class="pyramid-container">
      <div class="pyramid">
        <div class="base"></div>
        <div class="side side1"></div>
        <div class="side side2"></div>
        <div class="side side3"></div>
        <div class="side side4"></div>
      </div>
    </div>
  </div>

</body>
</html>
